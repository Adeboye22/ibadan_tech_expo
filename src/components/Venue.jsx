import Logo from '../assets/venue/venue.svg'
const Venue = () => {
    return (
        <section>
            <div>
                <img src={Logo} alt="" />
            </div>
        </section>
    )
}

export default Venue
